JS Back-End Exam - 28-June Setup:

npm - to install all dependencies

 "devDependencies": {
    "nodemon"
  },
  "scripts": {
    "start": "nodemon"
  },
  "dependencies": {
    "bcrypt"
    "body-parser"
    "cookie-parser"
    "express": 
    "express-handlebars"
    "express-validator"
    "jsonwebtoken"
    "mongodb"
    "mongoose"
  }
}

npm start - to run the web server locally

open localhost:3000/home
